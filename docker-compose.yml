version: '3.9'
services:
  homemgt-frontend:
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - .env.production
    restart: unless-stopped
    networks:
      - microservices-net
    

networks:
  microservices-net:
    external: true